#ifndef _CONFIG_VLAN_H
#define _CONFIG_VLAN_H

int cmd_exit(struct cli_context *, int, char **, struct menu_node **);
int cmd_namevlan(struct cli_context *, int, char **, struct menu_node **);

#endif
